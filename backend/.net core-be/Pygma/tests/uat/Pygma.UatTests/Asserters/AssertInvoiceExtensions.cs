namespace Pygma.UatTests.Asserters
{
    public static class AssertInvoiceExtensions
    {
        // public static void AssertUpsertInvoice(this InvoiceVm actual, IUpsertInvoice expected)
        //  {
        //      actual.InvoiceDate.Should().Be(expected.InvoiceDate);
        //      actual.RelatedInvoices.Should().Be(expected.RelatedInvoices);
        //      actual.PurposeOfTransport.Should().Be(expected.PurposeOfTransport);
        //      actual.CarNumber.Should().Be(expected.CarNumber);
        //      actual.LoadingRegion.Should().Be(expected.LoadingRegion);
        //      actual.DestinationRegion.Should().Be(expected.DestinationRegion);
        //      actual.ShippingMethod.Should().Be(expected.ShippingMethod);
        //      actual.PaymentMethod.Should().Be(expected.PaymentMethod);
        //      actual.IsVoid.Should().Be(expected.IsVoid);
        //      actual.HasParelkomena.Should().Be(expected.HasParelkomena);
        //      actual.OrderId.Should().Be(expected.OrderId);
        //  }
    }
}